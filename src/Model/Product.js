class Product {
  constructor(name, price, quantity, promotion, promotionCount) {
    this.name = name,
      this.price = price,
      this.quantity = quantity,
      this.promotion = promotion,
      this.promotionCount = promotionCount;
  }
}

export default Product;